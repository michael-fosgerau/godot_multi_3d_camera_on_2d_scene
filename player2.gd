extends KinematicBody

# A simple program to rotate the model around

#var model = null
#const SPEED = 40

func _ready():
	#model = get_node("Camera2")
	#set_process(true)
	pass;
	
func _process(delta):
	pass;
	#model.rotation_degrees.y += delta * SPEED